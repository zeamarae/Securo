{
  "name": "securo",
  "version": "1.0.0",
  "main": "auth.js",
  "scripts": {
    "dev": "npx serve -p 5508 .",
    "build": "node -e \"const fs=require('fs'); if(!fs.existsSync('www')) fs.mkdirSync('www'); fs.readdirSync('.').forEach(f=>{if(['.html','.js','.css','.png','.jpg','.svg'].some(ext=>f.endsWith(ext))) fs.copyFileSync(f, 'www/'+f);}); console.log('Assets copied to www');\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "puppeteer": "^24.42.0"
  }
}
